# Node
node_modules/
.next/
apps/web/.env
apps/web/.env.local
apps/admin/.env
apps/admin/.env.local

# Python
services/scraper/.venv/
services/scraper/__pycache__/
services/scraper/**/__pycache__/
services/scraper/*.egg-info/
services/scraper/.env

# OS / editor
.DS_Store
*.log

# Personal MCP server config — contains real API keys (n8n, etc.), not
# meant to be shared/committed.
.mcp.json

# n8n workflow exports — kept local only, not versioned in git.
n8n/

# SSH / deploy keys — never belong in the repo
yes
yes.pub
*.pem
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# Reference clone (has its own git history, not part of this repo)
Scrapegraph-ai/
