import { ComingSoonBanner } from "@/components/ComingSoonBanner";

export default function ProgressPage() {
  return (
    <ComingSoonBanner
      title="Progress dashboard"
      description="A unified view of scrape health and growth over time — trend charts, success rates, and historical run comparisons across every source. For now, live per-run progress still lives under Scheduler."
    />
  );
}
