import { ComingSoonBanner } from "@/components/ComingSoonBanner";

export default function DashboardPage() {
  return (
    <ComingSoonBanner
      title="Dashboard"
      description="An at-a-glance overview of jobs, contests, sources, and scraper health in one place. For now, use the tabs on the left to manage each area individually."
    />
  );
}
